# mamba main page test project as testing task

How to start:

clone repo

make run ```npm i```

For run tests:

```npx codeceptjs run```

For run tests with steps at console:

```npx codeceptjs run --steps```
